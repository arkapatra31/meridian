[project]
name = "meridian"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "claude-agent-sdk>=0.1.68",
    "python-dotenv>=1.0.1",
    "anyio>=4.0.0",
]
